class Cookbook {
  constructor(id, title, author, views, recipes, description, source) {
    this.id = id;
    this.title = title;
    this.author = author;
    this.views = views;
    this.recipes = recipes;
    this.description = description;
    this.source = source;
  }
}

export default Cookbook;
