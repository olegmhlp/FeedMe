import {StyleSheet} from 'react-native';

export const buttonStyles = StyleSheet.create({
  buttonContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 25,
  },

  buttonText: {
    fontSize: 18,
    marginLeft: 4,
    marginBottom: 2,
    color: '#393939',
  },
});
